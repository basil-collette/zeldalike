using System.Collections.Generic;
using System.Text;
using UnityEngine;

public static class DebugHelper
{
    /*
    public static void DebugList<T>(List<T> list, string property)
    {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < array.Count; i++)
        {
            sb.Append(array[i][property]);
            sb.Append(", ");
        }
        Debug.Log(sb.ToString());

        
    }

    public static void DebugArray<T>(<T>[] array, string property)
    {
        
    }
    */

}